{% extends 'base.html.twig' %}

{% block title %}Saison{% endblock %}

{% block body %}
    <h1>Saison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saison.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ saison.libelle }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ saison.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ saison.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_saison_index') }}">back to list</a>

    <a href="{{ path('app_saison_edit', {'id': saison.id}) }}">edit</a>

    {{ include('saison/_delete_form.html.twig') }}
{% endblock %}
